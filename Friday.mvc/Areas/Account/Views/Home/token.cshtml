@{
    Layout = null;
}
<script>
    (function (v, d, w, p) {
        try {
            //d.domain = d.domain.split('.').slice(-2).join('.');
            w.getToken = function () {
                return v
            };
            w._tb_token_ = v;
        }
        catch (e) {
        }

    }
   )
    ("464cKa3TVmwE", document, window, parent);
 </script>

